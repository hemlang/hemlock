// Test that string to bool conversion via type annotation fails
// This should error because type annotations don't parse strings
let s = "true";
let b: bool = s;  // Should error - use bool("true") instead
print(b);
