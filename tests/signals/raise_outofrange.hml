// Test raising out-of-range signal number
// Errors are catchable with try/catch
import { raise } from "@stdlib/signal";

try {
    raise(99999);
    print("Raising invalid signal did not error");
} catch (e) {
    print("PASS: Caught invalid signal: " + e);
}
