// @expect-compile-fail reassigning a const from an inner scope is a compile error
// Test that reassigning const in nested scope causes error
const x = 10;

{
    print(x);
    x = 20;  // Should error: Cannot assign to const variable 'x'
}
