// @expect-compile-fail reassigning a const is a compile error
// Test that reassigning const causes error
const x = 10;
print(x);

x = 20;  // Should error: Cannot assign to const variable 'x'
print(x);
