fn mkstr(n: i64): string { return "v" + n + "-payload"; }
let n = 0;
while (n < 2000) {
    let row = {};
    let c = 0;
    while (c < 5) {
        let key = "col" + c;
        let value = mkstr(n + c);
        row[key] = value;
        c = c + 1;
    }
    n = n + 1;
}
print("objset_leak done");
