/*
 * Hemlock Version Header
 *
 * Centralized version information for all Hemlock components.
 * All components (interpreter, compiler, LSP) share this version.
 */

#ifndef HEMLOCK_VERSION_H
#define HEMLOCK_VERSION_H

#define HEMLOCK_VERSION_MAJOR 2
#define HEMLOCK_VERSION_MINOR 10
#define HEMLOCK_VERSION_PATCH 2

#define HEMLOCK_VERSION "2.10.2"
#define HEMLOCK_VERSION_STRING "Hemlock v" HEMLOCK_VERSION

/*
 * Version history:
 *   2.10.0 - @stdlib/websocket and @stdlib/http work on Windows, and Windows binaries ship with the release. The WebSocket/HTTP builtins were never Windows-specific -- they compile and run there unchanged -- but the top-level Makefile hardcoded HAS_LIBWEBSOCKETS := 0 for every Windows build, so websockets_ws.c compiled its "not available" stubs and any program importing those modules threw at runtime. A native MSYS2 build now probes its own pkg-config (pacman -S mingw-w64-ucrt-x86_64-libwebsockets); cross builds still skip the probe, since a Linux host's pkg-config would answer about the wrong library. hemlockc links libwebsockets for compiled programs too, which it has to spell -Wl,-Bdynamic -lwebsockets: those link commands pass -static, under which the linker demands libwebsockets.a and MSYS2 ships an import library plus a differently-named libwebsockets_static.a ("have you installed the static version of the websockets library ?"). Fixed alongside: the WebSocket client's connect wait counted iterations of lws_service(ctx, 100) as if that argument were a 100ms block, but it has been ignored since lws 3.2 and returns immediately under the libuv event loop MSYS2 builds with -- so a documented 10-second timeout could expire in microseconds, and the first (coldest) connect in a process failed intermittently while every later one succeeded (~40% of runs against a live server; gn.hml's WebSocket suite went from 1-in-2 flaky to 0/20). It is paced with usleep now, matching what ws_server_accept in the same file already did correctly, in both backends. This is not a Windows-only bug: every lws >= 3.2, which is every current distro, has the same busy-spin. New: make WIN_LWS_STATIC=1 links libwebsockets and its TLS/event-loop dependencies into the binaries, 4.6 MB -> 12.2 MB, which is what the release job uses -- a dynamically linked build needs libwebsockets.dll, libssl-3-x64.dll and libuv-1.dll on PATH and exits without a word outside an MSYS2 shell. Release workflow now builds and ships hemlock-windows-x86_64.zip beside the Linux, macOS and WASM artifacts, verifying the binaries depend on nothing but system DLLs and round-tripping a WebSocket echo on a clean PATH. Also corrected: docs/advanced/windows.md described the UCRT throw-from-native-callback crash as ~1 in 25, measured from one sort-comparator fixture; exception-heavy compiled code hits it in 40-60%% of runs (error_catchable: 8/15 on pristine main), so the note now reports the real severity and scope.
 *   2.9.1 - array.sort() is now a stable bottom-up merge sort in both backends, replacing a Lomuto quicksort (compiler) and an insertion sort (interpreter) that were each quadratic on inputs programs routinely produce. The compiled runtime's quicksort took a last-element pivot with no randomization and recursed on both halves, so sorted or reverse-sorted input hit the worst case in both dimensions at once: O(n^2) comparisons AND O(n) recursion depth. A presorted 200k-element array segfaulted from stack exhaustion, and 2000 elements took ~5ms instead of ~0ms — and sorted input is not an exotic case, it is what any incremental workload (re-sorting a mostly-ordered list each frame or each tick) produces by construction. The interpreter's insertion sort was the mirror image: linear on presorted input, quadratic on shuffled. The two backends also disagreed on equal-comparing elements, since quicksort is unstable and insertion sort is stable, so sort() had no usable stability contract. Both now run the same iterative merge sort: O(n log n) comparisons on every input with no quadratic worst case, no recursion so no array size can exhaust the C stack, and stable — which makes tie order a documented guarantee rather than a backend-dependent accident. Each merge first checks whether its two runs are already in order and skips the copy when they are, keeping fully sorted input at O(n) comparisons and zero data movement. Presorted 200k now sorts in 1ms (was: segfault); presorted 2000 drops ~5ms to ~0ms; 50k shuffled sorts in 7ms interpreted. Two correctness properties come with the rewrite: each merge builds its output in scratch space and copies back only once complete, so a comparator that throws leaves the array a valid permutation of its elements instead of a buffer with duplicated slots and corrupted reference counts (the old interpreter path returned mid-shift and lost an element); and a comparator that resizes the array being sorted — which would dangle the element pointer the merge is walking — now raises a catchable error instead of a use-after-free. Known limitation, unchanged in kind from map()/filter(): in compiled code a throwing comparator longjmps past the sort and leaks the scratch buffer, because hml_throw() neither unwinds nor runs defers, so there is no cleanup hook to attach the free() to. New tests/parity/methods/array_sort.hml covers default and custom comparators, stability, presorted/reversed/shuffled input, edge cases, and the throwing-comparator path; verified clean under valgrind on both backends. Suites: 332/332 parity, 54 compiler.
 *   2.9.0 - Semantics-hardening release: the documented contracts are now actually true on both backends, because each was formalized, audited against the implementation, and fixed where it disagreed. ARITHMETIC: the checked-arithmetic contract (i32/i64 add/sub/mul throw catchable "Integer overflow", narrow and unsigned wrap, `/` always returns float) was full of holes that made the same expression behave differently depending on whether it was written as literals or variables, inside a string concatenation, or run through the compiler. The frontend optimizer no longer folds i32/i64 overflow (2147483647 + 1 folded to an i64 instead of throwing), folds u64 literals in uint64, compares 64-bit ints in integer precision rather than through double, folds shifts with i32 width semantics, and no longer applies semantics-changing rewrites (x/1 returned an int where / must return float; x+0 / x*1 / x|0 changed typeof for narrow operands; !!x and -(-x) leaked non-bool values and skipped negation overflow; x && true returned the operand where && always returns bool). The interpreter's hidden integer-division mode inside string concatenation is gone ("x" + 7/2 gave "x3" interpreted, "x3.5" compiled), INT_MIN % -1 is 0 instead of a process-killing SIGFPE, and negating INT32_MIN/INT64_MIN throws instead of wrapping. Codegen's native fast paths emit overflow-checked code for i32/i64 (raw C signed arithmetic was UB and skipped the throw) and fall back to the runtime path whenever raw C semantics differ from promoted-type semantics; codegen's private constant folder was deleted (it re-folded deliberately-unfolded expressions with the old broken semantics); both build trees and all generated C now compile with -fwrapv so documented wrapping is defined rather than UB. TYPES: variable annotations are enforced on reassignment in both backends — `let x: i32 = 7; x = "hello";` silently rebound x in the interpreter and dynamic values bypassed the compiler's static check entirely, since only initial declarations were ever checked. MEMORY SAFETY: new docs/design/memory-safety-model.md (RustBelt-style invariants B1-B8, safe fragment, memory-safety theorem, per-crossing proof obligations for every ptr operation) whose audit found real counterexamples reachable from buffer-only code, all fixed: free() with a live slice view left the view dangling (roots now carry an atomic view_count and refuse the free), overlapping buffer-to-buffer copies invoked C memcpy on overlapping ranges (now memmove), buffer(0) hard-exited uncatchably in the interpreter while compiled code returned a 0-length buffer, buffers were malloc'd in one backend and calloc'd in the other despite docs promising zeroing, and buffer_ptr() on a freed buffer silently returned NULL. Separately, interpreter bounds checks did not stop the access: runtime_error() does not unwind, so b[-1] = 9 reported the error and still stored to data[-1] (smashing the malloc chunk header) and b[0] after free(b) still dereferenced NULL. CONCURRENCY: new normative docs/advanced/memory-model.md (value taxonomy, six happens-before edges, race-freedom theorem, and the rule that shared bindings are not a communication channel), whose writing exposed two unbuffered-channel rendezvous bugs — a second concurrent sender staged over an already-staged value (dropping a message, leaking its reference), and multi-sender rendezvous detected success as sender_waiting == 0, which a re-staging sender plus a mis-delivered cond_signal turns into a permanently asleep sender; both fixed with a per-channel rendezvous generation counter. CODEGEN: a systematic differential audit found and fixed 30+ interpreter/compiler divergences — concat-chain left-associativity (1 + 2 + "x" compiled to "12x"), unsigned/narrow operands collapsing into signed fast paths (u64 >> u64 went negative), OR-patterns with bindings failing to compile, define-typed patterns matching any object, break inside a loop nested in a switch exiting the switch, eager instead of lazy case-value evaluation, jumps out of try skipping finally, labeled break/continue skipping unboxed write-back, try-body locals indeterminate after longjmp, rune lets unboxing to i32, named arguments bound positionally by the inliner and dropped by method calls, ref arguments corrupting the stack for unboxed variables and losing mutations for element/property refs, and receiver-type guards for string/file/channel-only methods so user objects with those method names dispatch. Plus: compiled `ptr == null` is now true for a NULL pointer (every FFI null-guard was dead code in compiled builds), finally runs when catch throws, extracted methods keep self bound, nested closures share captured variables by reference instead of a snapshot copy, and inlining is declined when a parameter name collides with an unboxed caller local (which emitted uncompilable C). REFCOUNTING: audit of both backends — ref-param double release (deterministic use-after-free), defer leaking results and clobbering the enclosing return value, VAL_WEBSOCKET missing from refcounting, caught exceptions leaking stack frames until "Maximum call stack depth exceeded", unrefcounted socket handles leaking per accepted connection, hml_throw leaking one reference per throw, s[i] = c mutating the shared ASCII literal pool, and codegen not owning parameters, catch parameters, or closure captures across early exits. TOOLING: new `hemlock check [--json]` subcommand runs every static pass (syntax with multi-error recovery, lint, type check, borrow check) without executing and reports all diagnostics rather than stopping at the first failing pass; the LSP publishes real per-error syntax diagnostics with the parser's own messages instead of one generic marker; docs/reference/stdlib-api-index.md is a generated one-line-per-symbol index of all 1050 stdlib exports (make stdlib-api, freshness-verified by the new make docs-check); `hemlock -e` executes imports (it never routed through the module system); and a parser segfault on import/export with a missing module path is fixed in every consumer. Suites: 765 interpreter, compiler, 331/331 parity, 23 contracts, 8 check, docs-check.
 *   2.8.2 - Compiler-diagnostics and runtime-correctness batch. RUNTIME: fixed a data race in the single-char ASCII string pool. init_ascii_strings() guarded a 128-entry table fill with a plain `static int initialized` and is called from hml_val_string() on the hot path for every single-character string, so two threads could each observe 0 and both run the fill (double-allocating all 128 immortal entries and orphaning one set), and because `initialized = 1` was written AFTER the fill with no release barrier a third thread could observe the flag set and read a not-yet-published entry — a garbage HmlString*, i.e. use of uninitialized memory, not merely a sanitizer complaint. ThreadSanitizer flagged it at src/value.c:253 under the concurrent_object_read stress test; it is timing-dependent, so the lane was usually green. Fixed by deleting the runtime initialization rather than synchronizing it: the pool is now a statically-initialized HmlString[128] built by macro expansion, each entry's `data` pointing at its own inline_data (an address constant, valid in a static initializer). That makes the race impossible by construction rather than unlikely, and drops 128 startup mallocs plus the per-call init branch from a hot path; the entries were already immortal and never freed, so static storage matches their real lifetime. Second bug found in this pool (2.4.6 fixed the immortal-sentinel countdown) — a fixed table of constants had no business being built at runtime. COMPILER: --strict-types was unusable on any multi-file project — running it over a ~19k-line codebase (Witchgrid) produced 96 "identifier has unknown type" warnings, all spurious, all anchored at line 0. Three causes: (1) type_check_stmt had no STMT_IMPORT case, so imported names fell through to `default: break;` and resolved to nothing — visible only where inference actually runs, so `"" + NAME` in a concat warned while a bare call did not; imports now bind ANY, since modules are type-checked separately and the exported type is not knowable without cross-module inference. (2) expr_ident() defaults line/column to 0 and the parser never stamped them, so every identifier-anchored diagnostic printed "file:0" (statements were unaffected, which is why lint warnings looked correct). (3) a forward reference from a function body to a module-level let/const declared further down the file was reported unknown, though the body only runs after module init; top-level let/const names are now collected before checking — names only, so redeclaration and shadowing checks are unchanged. Same codebase now reports 0, and a genuinely undefined identifier still warns, at its real line. TESTS: compile-failure fixtures declare themselves with `// @expect-compile-fail <reason>` in their first 20 lines instead of being tracked by a hand-kept path list inside run_repo_compile_check.sh — the expectation now lives next to the code encoding it, so adding a negative fixture no longer silently breaks CI until someone edits a shell script. All 20 converted and both directory rules dropped (they covered one file and zero files); classification verified identical before/after across every .hml in the tree. New parity fixture walks all 127 non-NUL ASCII pool entries, which is what catches a bad macro expansion that ordinary tests (touching a handful of characters) would miss. Also includes the previously-unreleased #613/#614 work: interprocedural borrow-checker consume detection + container escape and channel/task/FFI/mmap tracking; a static lint pass (unreachable code, dead branches, self-assign, % 0, self-compare, dup-field, dup-case, infinite loops that never fall through); and rejection of unknown named arguments at compile time — previously the type checker skipped them and only the interpreter threw, while the compiled binary silently bound the argument positionally, so the same program behaved differently on the two backends. Suites: 759 interpreter, 54 compiler, 304/304 parity, 18 lint, stress 11/11 under both tsan and asan.
 *   2.8.1 - Windows: hash builtins (sha1/sha256/sha512/md5 — and @stdlib/hash on top of them) now work natively, backed by Windows CNG (bcrypt.dll, a system DLL — binaries stay self-contained) instead of throwing HEMLOCK_NO_OPENSSL errors. exec()/exec_argv() also work now (CreateProcess + pipes via hml_win32_run_capture in the platform layer): shell mode through %COMSPEC% /S /C like popen through /bin/sh, argv mode direct with CommandLineToArgvW quoting, captured stdout/stderr, stdin: feeding, and POSIX-mirroring exit-code-127 objects on launch failure — unlocking @stdlib/shell run()/run_capture() on Windows (Unix-command helpers like ls()/which() stay Unix-only). NEW __term_* builtins (is_tty/raw/read_byte/size, src/shared/term_core.c: POSIX termios+poll+TIOCGWINSZ, Windows console API with VT input/output so ESC-sequence parsing and ANSI output work unmodified) put @stdlib/termios raw mode and @stdlib/terminal on Windows; terminal.hml stops shelling out (stty size → __term_size, per-frame __exec("printf ...") → write()). Compiler fix: imports nested in if/else platform gates emitted _ffi_lib_<name> loads without declaring the global (any program importing @stdlib/termios failed to compile); declaration pass now recurses. No-FFI builds (HEMLOCK_NO_FFI/WASM): import/extern-fn statements warn instead of throwing so platform-gated-FFI modules load. @stdlib/regex works on Windows: musl 1.2.5's TRE regcomp/regexec vendored (BSD-2, src/shared/regex_win32 + regex_compat.c unity TU, include path supplies <regex.h> on MinGW only) — regex builtins' POSIX code path now compiles on every platform, stubs deleted; full regex parity output matches Linux under Wine in both backends (only TRE-vs-glibc error wording differs). posix_spawn/waitpid/kill work on Windows (CreateProcess detached spawn with env/cwd/stdio-fd/setsid options; pid->handle registry = zombie analogue, no pid-reuse race; waitpid blocking/WNOHANG with POSIX-encoded status so status>>8 is portable; kill 0 probes, other signals terminate with 128+sig) — only fork/wait-for-any/uid family remain POSIX-only. stdlib waitpid became a defaulted-options wrapper (bare __waitpid re-export had fixed arity 2 compiled; documented 1-arg form only worked interpreted; parity test added). Gap-hunting tests added (import-all-stdlib, argv-quoting hazards, >64K dual-stream exec output, builtin-method-name shadowing) which surfaced + fixed three compiler bugs that kept @stdlib/mmap interpreter-only forever: let-bound module fns called from object methods emitted nonexistent _modN_fn_* symbols; __mmap_open's function-value num_params/num_required were swapped; compiled method dispatch sent .read_u8/.write_u8/.read_bytes/.write_bytes on ANY receiver to the buffer builtins and discarded .close()'s object-fallback return — now type-guarded with hml_call_method fallback. tests/stdlib_mmap passes identically in both backends. Also NEW: __random_bytes(n) CSPRNG builtin (BCryptGenRandom on Windows, /dev/urandom elsewhere; both backends + parity test) — @stdlib/crypto random_bytes() drops its OpenSSL RAND_bytes dependency, and @stdlib/uuid calls the builtin directly instead of importing @stdlib/crypto, fixing UUID generation on Windows (crypto's module-level libcrypto import can't load there); @stdlib/path expand_user() resolves ~ via __homedir() (USERPROFILE on Windows) instead of $HOME//home/<user> guessing. Implemented as hml_win32_hash() in src/shared/platform_win32.c (BCryptOpenAlgorithmProvider/CreateHash/HashData/FinishHash, chunked for >4 GB inputs since BCryptHashData takes a ULONG), declared in hemlock_compat.h (the windows.h-free header — the interpreter's internal.h can't include windows.h because winnt.h's TokenType collides with the lexer's), and wired into both backends: interpreter builtins/crypto.c gains an #elif _WIN32 CNG branch (its hash stubs dropped from wasm_interp_shim.c on Windows; ECDSA stubs remain — CNG emits raw r||s signatures where the OpenSSL builtins produce DER, no compatible mapping), runtime builtins_crypto.c gains the same branch for compiled programs. -lbcrypt added to every Windows link: hemlock.exe, hemlockc.exe, runtime LDFLAGS, and both generated hemlockc link commands (static + default). CI smoke tests now assert sha256("hello")/md5("hello") digests in all four lanes (native + Wine, interpreter + compiled). docs/advanced/windows.md crypto limitation row reduced to ECDSA-only.
 *   2.8.0 - Windows (MinGW-w64) support: hemlock + hemlockc + libhemlock_runtime build for Windows, cross-compiled from Linux/macOS (`make mingw`) or natively under MSYS2 (auto-detected); binaries are self-contained (only system DLLs). Platform layer: include/hemlock_platform.h (winsock2/afunix sockets, dlopen→LoadLibrary, mmap→CreateFileMapping/VirtualAlloc, poll→WSAPoll, socket-errno mapping, missing SIG_, PROT_, and MADV_ constants) + include/hemlock_compat.h (lean realpath/strndup/getline/mkstemps/setenv decls — safe without windows.h, whose TokenType enumerator collides with the lexer's) + src/shared/platform_win32.c, compiled into both backends; file I/O defaults to binary mode so byte semantics match POSIX. Works on Windows: language core, async/spawn/channels/atomics (winpthreads), TCP/UDP/AF_UNIX sockets + DNS, file/dir I/O, mmap, zlib, and the stdlib built on those; module resolution understands drive-letter absolute paths. FFI fully supported when MinGW libffi is present (auto-detected; MSYS2 package or one-time cross build — CI does both): DLLs load via LoadLibrary, extern fn dispatches through statically-linked libffi, and Linux lib names translate automatically (import "libraylib.so" finds libraylib.dll then raylib.dll) so raylib-style bindings build into Windows game exes unmodified. POSIX-only features throw clear runtime errors: fork/exec/posix_spawn/kill/uid family, POSIX regex, OpenSSL hashes (HEMLOCK_NO_OPENSSL), and FFI only when libffi is absent (HEMLOCK_NO_FFI; stubs reuse the WASM shim). hemlockc's system() invocation of gcc goes through cmd.exe on Windows — shell_quote now emits CRT-style double quoting there (POSIX single quotes were passed literally; also refuses %/!/quote chars cmd can't escape inside quotes), found by the new windows-native CI job. CI: windows-mingw.yml cross-builds + Wine-smoke-tests both backends AND builds natively on a real Windows runner (MSYS2/UCRT64) exercising the full hemlockc→gcc pipeline; -Werror=implicit-function-declaration on Windows builds keeps the GCC 13 cross toolchain as strict as native GCC 14 (implicit decls of the compat shims truncated pointers on x64). POSIX builds unchanged — every platform difference is behind #ifdef _WIN32 / feature gates; parity 282/282.
 *   2.7.0 - Language ergonomics + parity batch driven by production-consumer feedback (gn.hml, Witchgrid). NEW: object-literal method shorthand `{ fn name() {...} }` (sugar for `name: fn() {...}`; async + expression-bodied forms work; gn.hml had to write every callback as `name: fn()`); side-effect imports `import "./suite.hml";` / `import "@stdlib/x";` (bare source-path imports load+execute without binding — was reserved for FFI loads, the documented workaround was `import {} from`; Option B of docs/proposals/source-side-effect-imports.md, formatter normalizes the old spelling); `string.rfind(needle)` (last occurrence — basename extraction was split("/")+last); one-arg `substr(start)` (to end of string); `@stdlib/strings from_bytes()` (documented replacement for the internal __string_from_bytes that both known production consumers reached for); `@stdlib/bytes` f32/f64_to_bits/from_bits IEEE 754 bit casts (Witchgrid's GGUF parser skipped float metadata for lack of them). FIXED: compiled substr()/slice() used BYTE offsets where the interpreter uses CODEPOINT offsets — any multibyte string produced invalid-UTF-8 fragments and interpreter/compiler divergence (the "substr garbage" Witchgrid pinned); find() now returns the codepoint index it was always documented to return (was byte index — composes with substr/slice/char_at; ASCII behavior unchanged); parser infinite loop/OOM ("Killed", no diagnostic) when a statement inside any block errored without consuming a token (e.g. `fn(self)` — block_statement gained parse_program's forward-progress guard); formatter crash on star imports (`import * from`, NULL namespace_name). 6 new parity tests.
 *   2.6.0 - Static-by-default linking on Linux for hemlockc-compiled programs. libwebsockets does not keep its lws_context_creation_info struct ABI-stable across releases — between 4.0 (Ubuntu 22.04) and 4.3 (Ubuntu 24.04) it REORDERED fields (port 0→96, options 104→440, protocols 16→8, …), so a dynamically-linked Hemlock binary built against one libwebsockets crashes (SIGSEGV in lws_create_vhost) the moment it makes any HTTP/WS call against a different one. Diagnosed via Witchgrid (its agent died on its first /register POST on a 24.04 runner) and confirmed with offsetof() across both libs. Over-zeroing the struct can't fix reordered offsets; the only real fix is to not depend on the host's libwebsockets ABI. So hemlockc now defaults to the hybrid-static link (-Wl,-Bstatic -lffi -lwebsockets -lssl -lcrypto -Wl,-Bdynamic -lcap -luv -lev -lm -lpthread) on Linux, producing self-contained, portable binaries. Compile-time FFI (extern fn — @stdlib/sqlite et al.) still works (libsqlite3 etc. stay dynamic, which is fine — they ARE ABI-stable); only runtime FFI (ffi_open/ffi_bind) is unavailable under static linking. Opt out with the new --dynamic flag. macOS stays dynamic by default (consistent system libraries, awkward brew static archives). NOTE: a 22.04-built Hemlock release still grabs the *build host's* libwebsockets.a, so for full build-host independence the release should additionally bundle its own static libs (tracked follow-up); pinning the build host to match the release env is the current guidance.
 *   2.5.7 - Two resource-leak fixes for long-running concurrent / server programs (the Witchgrid agent + control-plane daemons are the motivating workload). (1) runtime/src/value.c task_free() now pthread_detach()es a task whose handle is dropped without an explicit join()/detach() — previously the joinable pthread's resources were never reclaimed even after the task finished, so a program that spawns tasks and drops their handles slowly leaked thread state (compiled runtime; surfaced by a ThreadSanitizer leak in the concurrency stress suite, PR #583). (2) SocketHandle gains an atomic ref_count with socket_retain()/socket_release() (include/runtime/types.h + memory.h, src/backends/interpreter/builtins/net.c, src/backends/interpreter/values.c), wired through the accept path and socket value lifetimes, so accepted per-connection sockets are freed instead of leaked — closing the ~50 B/connection accepted-socket gap that was explicitly tracked as "needs a socket-lifetime refcount change" since 2.4.10/2.4.11 (interpreter backend, PR #584).
 *   2.4.11 - Fix `obj[key] = value` (and any indexed assignment) leaking the RHS — the pervasive leak that LSan-on-the-real-CP finally pinned. The index-assignment codegen released the object and index temps (hml_release_if_needed) but never released the value temp: codegen_expr returns the RHS owned (+1), every setter (hml_object_set_field / set_field_coerce / array_set / string_index_assign / buffer_set / ptr_set) takes its OWN reference, so the RHS creation reference was orphaned — every indexed store leaked its value. @stdlib/sqlite query() does `row[col_name] = value` for every column of every row, so a server that queries its DB per request (the Witchgrid control-plane: nodes_for_dashboard / compute_stats / etc. on every dashboard poll) bled GB; the per-CP-request leak was ~175 KB. Found by LSan-instrumenting the actual witchgrid-cp binary (emit-c + -fsanitize=address + a watchdog exit so LSan reports) and hammering it — the stacks named hml_cstr_to_string ← get_column_value ← query directly, ending a long synthetic-repro chase. Fix: release the RHS temp after the setter (it took its own ref), mirroring the array-literal `push; release(&elem)` pattern. Verified: a `row[k]=v` reproducer 560,056 → 56 bytes; parity 259/259; stress 6/6; new tests/stress/objset_leak.hml on the CI lsan lane. NOTE: a separate ~50 B/connection residual remains — accepted sockets are never freed (HmlSocket has no refcount; HML_VAL_SOCKET absent from hml_release, "OS resource shared by reference" by design); needs a socket-lifetime change, still tracked.
 *   2.4.10 - Fix every parsed JSON object leaking its entire tree (the real, dominant multi-GB Witchgrid control-plane bleed — the for-in fix in 2.4.9 was necessary but not the main CP leak). json_parse_object() hand-builds the HmlObject with malloc and sets 8 fields but never initialized is_pooled — so it held indeterminate stack/heap garbage. object_free() branches on `if (obj->is_pooled) obj_pool_free(obj); else free(...)`; with garbage is_pooled the foreign malloc'd object was routed into obj_pool_free() (which no-ops on non-pool blocks) and never freed — and the object pool freelist corrupted. Arrays were unaffected (no is_pooled), and object *literals* were fine (hml_val_object sets is_pooled=0); only the serializer's two hand-built object branches (empty-object and populated-object) were broken. Pinpointed by an HTTP-client+JSON LSan workload harness modelling the dashboard fan-out (outbound get + parse + traverse, per agent per poll): the leak was 2,679,006 bytes/1500 reqs and dropped ~36x to 75 KB after setting obj->is_pooled = 0 in both branches. Parity 259/259; stress 6/6. NOTE: the ~75 KB residual is a separate, much smaller (~50 B/connection) gap — accepted sockets (hml_socket_accept) are never freed because HmlSocket has no refcount and HML_VAL_SOCKET isn't in hml_release (sockets are "OS resource, shared by reference" by design); fixing it needs a socket-lifetime refcount change and is tracked next.
 *   2.4.9 - Fix the for-in iterable refcount leak for ALL iterable kinds — the actual multi-GB Witchgrid control-plane bleed. codegen_expr already returns an owned (+1) temporary for every iterable shape (idents/index/member get an hml_retain_if_needed'd copy, calls return their owned result, literals are freshly built); that +1 keeps the collection alive for the whole loop even if the body reassigns the source, and the single hml_release(&iter_val) at loop end balances it. The for-in additionally emitted its own hml_retain(&iter_val) with no second release, so every `for x in <iterable>` leaked the entire collection + contents on each execution. 2.4.7 removed the redundant retain only for the EXPR_CALL subcase (obj.keys()); idents, array/object/string literals, index and member iterables still leaked — e.g. `for tk in ts` once per request grew a server unbounded (the spawn/await path blamed in 2.4.8's note was a red herring: spawn/await itself is leak-clean; the tasks only leaked because the `ts` array holding them was leaked by its own for-in). Fix: drop the for-in's extra retain entirely; rely on codegen_expr's owned temp + the existing end-release. Verified with LSan: reproducers that leaked 64 KB–780 KB now hit the 56-byte clean baseline; parity 259/259; new tests/stress/forin_leak.hml guards ident/literal/keys/spawn-into-array shapes under the CI lsan lane.
 *   2.4.8 - Fix a silent infinite loop from a codegen state leak (correctness, not a leak — the scariest failure mode found in the audit). funcgen_add_params() set ctx->func_params for the function being generated, but FuncGenState's save/restore never tracked func_params/num_func_params/func_param_is_ref — so after ANY function body was generated, that function's parameter names stayed in ctx->func_params forever. codegen_is_func_param() is name-keyed and unscoped, so at top level it then reported those names as "function parameters". codegen_expr_ident gates the while-loop unboxing read path on `!codegen_is_func_param(name)`; with the stale entry, a top-level `while (n < 5) { n = n + 1 }` whose counter happened to share a name with some function's parameter (e.g. a defined-but-maybe-uncalled `fn f(n: i64)`) had its in-loop reads of `n` fall back to the boxed `_main_n` while the unboxed local was still declared and only synced back AFTER the loop — so the condition read a never-updated value and spun forever. Fix: add func_params/num_func_params/func_param_is_ref to FuncGenState; funcgen_save_state saves + clears them, funcgen_restore_state restores them, so a function's params can never leak into the enclosing or top-level scope. Found while leak-hunting (an arg_leak repro named a param `n`); pinned by reading the emitted C. New parity test tests/parity/language/while_unbox_param_name_collision.hml (parity 259/259). NOTE: the spawn/await HmlTask-lifetime leak — the actual multi-GB Witchgrid CP bleed — is still open and tracked next in the codegen-ownership audit.
 *   2.4.7 - Fix a large refcount leak in `for k in obj.keys()` (and any `for x in <call>()`). Two compounding bugs, both pinpointed with the new LeakSanitizer stress lane: (1) codegen for-in always retained the iterable then released it once at loop end — net-zero, correct for a borrowed `for x in somevar`, but for an owned call temporary like obj.keys() the call's original +1 was never consumed, so the whole keys array + backing store leaked every iteration. Fixed: when the iterable is an EXPR_CALL the redundant retain is skipped so the single end-release consumes the call's owned reference (gated to EXPR_CALL — a call never returns a borrow, so no double-free risk; nothing aliases the temp so it can't be freed mid-loop). (2) hml_object_keys() pushed hml_val_string() temporaries into an array, but hml_array_push() RETAINS its argument, orphaning each string's rc=1 creation reference — so every key string leaked even after the array was freed. Fixed: release the temp after pushing. Net on a render loop calling caps_data.keys()/binaries.keys()/flags.keys() per request: leak went from ~1.7 MB / 24k allocations per 2000 renders to 0 (one benign one-time hml_get_args argv alloc remains, suppressed). A Witchgrid control-plane bled multiple MB/min through this exact path. Parity 258/258; new tests/stress/keys_leak.hml guards it under CI's lsan lane. NOTE: the lsan tooling also surfaced separate, larger pre-existing refcount leaks in the broader concurrency paths (e.g. concurrent obj[key]+string-concat) — tracked for an ongoing codegen-ownership audit; the lsan lane is deliberately scoped to *_leak.hml so a real regression isn't masked by known-unfixed leaks.
 *   2.4.6 - Fix the immortal ASCII-string pool being freed after exactly ~1,000,000 releases. The pre-allocated single-char string pool (init_ascii_strings) marked entries "immortal, never freed" by setting ref_count = 1000000 — but hml_retain/hml_release still did the normal atomic add/sub with no immortal check, so the sentinel was a countdown, not a freeze. Any program that materializes-then-releases single-char strings ~1e6 times — trivial for a busy server: every `"x" + s` allocates the pooled "x" and releases it — drove the count to 0, string_free()'d a pooled string the process-global cache still points at, and the next single-char string use was a heap-use-after-free in hml_string_concat (SIGSEGV / "double free"). This is the deeper bug noted in 2.4.5: it is NOT concurrency (reproduces single-threaded after enough iterations); the Witchgrid control-plane just reached the threshold every ~73 min under dashboard load. Fix: HML_REFCOUNT_IMMORTAL sentinel + HML_REFCOUNT_IMMORTAL_MIN threshold; hml_retain/hml_release now check the count *before* the atomic op and treat frozen objects as immovable — never incremented, never decremented, never freed. Pinpointed with an ASan-instrumented runtime build (the alloc/free/use stacks named init_ascii_strings directly). Verified: a 10,000,000-iteration single-char-concat reproducer that crashed at ~1-2M now survives; parity 258/258 unchanged.
 *   2.4.5 - Thread-safety fix: object field lookup is now lock-free read-only and never lazily mutates a shared object. object_lookup_field() used to build the hash table on first lookup ("if no hash table, object_hash_rebuild(obj)"); JSON-parsed and some literal-constructed objects reach readers with hash_table == NULL (see builtins_serialization.c "Lazy initialization"), so N concurrent obj[key] reads of the same shared object all entered the rebuild and raced on free(obj->hash_table)+malloc — heap corruption surfacing as "free(): double free / unaligned chunk" and SIGSEGV. Now the read path falls back to a lock-free linear scan when no hash table is present (objects are typically small; correctness over a micro-opt that corrupts the heap); the hash table is still built eagerly by set_field/delete_field for the mutation path's speed. Diagnosed via the 2.4.4 crash handler on a Witchgrid control-plane core-dumping every ~73 min: a JSON-parsed capabilities map indexed concurrently by spawn-per-request dashboard handler threads. NOTE: a separate, deeper issue remains — codegen refcount ownership for a module-global string used as a concat operand over-releases (and slow-leaks) it, crashing single-threaded after enough iterations; tracked separately, needs a careful codegen fix guarded by a concurrency stress / ThreadSanitizer harness.
 *   2.4.4 - Fatal-signal backtrace handler in the runtime. hml_runtime_init now installs handlers for SIGSEGV/SIGABRT/SIGBUS/SIGFPE/SIGILL that dump a symbolized backtrace (glibc backtrace_symbols_fd, async-signal-safe) to stderr, then restore the default disposition and re-raise so the original exit status / core-dump / systemd Restart= behavior is preserved exactly. The compiler now links binaries with -rdynamic (all link paths: Linux dynamic/static ±websockets, macOS) so the binary's own Hemlock functions resolve to names in the trace, not bare addresses — turns "systemd says status=11/SEGV, no idea where" into an actionable stack on the very first crash, with zero gdb/valgrind/coredump tooling required on the target box. Opt out with HML_NO_CRASH_HANDLER=1 (e.g. when a program installs its own SIGSEGV handler via the signal() builtin). Motivated by a Witchgrid control-plane that was core-dumping every ~73 min on a box with no debugger installed; the handler is what makes that diagnosable in-place.
 *   2.4.3 - Partial fixes for macOS-only sleep() timer coalescing on idle daemons. Improves but does not fully eliminate the kernel-level behavior where a quiet long-running process's sleep(N) calls return N to ~1000 seconds late. Three changes: (1) spawn'd-task threads now pin themselves to QOS_CLASS_USER_INITIATED via pthread_set_qos_class_self_np in builtins_async.c task_thread_wrapper — without this they inherit the process QoS and a launchd-spawned daemon can land in a coalesced class. (2) hml_runtime_init now resets nice to 0 via setpriority(PRIO_PROCESS, 0, 0) and pins the main thread's QoS, undoing the silent nice +5 that nohup adds on macOS shells (macOS auto-downgrades QoS for any process with positive nice, even though that fact isn't documented anywhere obvious). (3) hml_sleep on macOS now uses libdispatch's DISPATCH_TIMER_STRICT timer source — Apple's documented opt-out from coalescing — instead of nanosleep(). End-to-end this fixes the FIRST few minutes after a fresh launch (heartbeats fire on time, every interval, exactly as requested); after the process has been quiet for several minutes even DISPATCH_TIMER_STRICT gets stretched, indicating remaining throttling is process-level rather than per-thread/per-call. Verified end-to-end with a witchgrid-agent on M1 mac mini: clean 30s heartbeats post-launch, intermittent stalls thereafter.
 *   2.4.2 - Four macOS-only Hemlock bugs (all surfaced via a long-running HTTP server + async sqlite + periodic outbound HTTP, none reproduce on Linux). (1) @stdlib/sqlite.open_db() now passes SQLITE_OPEN_FULLMUTEX so the connection is safe to use from spawn'd handlers — macOS system libsqlite3 defaults to "multithread" (no internal serialization) and segfaults in sqlite3DbMallocRawNN when a query fires from another thread; Linux glibc happens to default to serialized so the same code worked there. New SQLITE_OPEN_NOMUTEX export for the explicit opt-out via open_db_flags(). (2) LWS_SERVER_OPTION_DO_SSL_GLOBAL_INIT is now set only once per process via a static once-flag — passing it on every lws_create_context (the natural pattern for one-shot HTTP requests) made the second context's lws_context_init_client_ssl fail and tear down the partially-built default vhost; on macOS that teardown closed pollfds in a way that corrupted unrelated TcpListener fds in other threads. (3) hml_socket_create / hml_socket_accept now F_DUPFD to >= 3 when the kernel returns a low fd. libwebsockets' Homebrew build pulls in LWS_WITH_PLUGINS_BUILTIN, which auto-loads sshd / raw-test demo protocols on first lws_create_context; those plugins close stdin when their config files are missing, so the next socket() lands on fd 0 — and the next lws context's default-vhost teardown closes fd 0 again, killing any HTTP-server listener that landed there (accept() spins on EBADF forever). (4) New hml_runtime_pin_stdio() re-pins /dev/null onto fds 0/1/2 if any are closed — called at runtime startup AND before every lws_create_context. Without this, lws's open of /dev/urandom landed on fd 0 (closed by the sshd plugin) and was rejected ("ZERO RANDOM FD"), leaving OpenSSL with a degraded entropy source that intermittently stalled SSL/TLS-adjacent setup for 3-9 minutes per call — outbound heartbeats from a witchgrid-agent went silent for minutes at a time before resuming.
 *   2.4.1 - Three follow-ons from Witchgrid agent-restart adoption work: TCP listener + accepted-client socket fds now set FD_CLOEXEC in hml_socket_create + hml_socket_accept (without this, posix_spawn'd children inherited the listener and held it alive across the parent's death, blocking same-port restart); new file.read_binary() on @stdlib/fs.open(...) returning a buffer to preserve nul bytes when reading /proc/<pid>/cmdline or other binary content (mirrors stream.read_binary from 2.3.1); exec_argv() gained a stdin option that pipes a string into the child's stdin via pipe(2), removing the `sh -c "cmd < file"` wrapper pattern. Plus the macOS Makefile pkg-config-fallback patch from the mac-build-docs branch (probes /opt/homebrew/include + /usr/local/include).
 *   2.4.0 - HTTP/auth + ergonomics batch (10 issues from Witchgrid bring-up): @stdlib/http now threads custom headers through POST/PUT/DELETE/PATCH and the timeout variants (was: only Content-Type made it to libwebsockets, breaking Authorization on POST); interpreter named-module imports are now live bindings instead of import-time snapshots so reassigning an exported `let` is visible to importers and to spawned tasks; flow null-narrowing propagates through `?.` field access (no more `let mp = model_path` shadow-rebind after a null check); type error labels report the parameter name instead of `'positional'`; `string.lower()`/`upper()` aliases for `to_lower`/`to_upper`; `get_binary` follows 3xx redirects (GitHub→CDN tarball pulls work directly); C codegen no longer prefixes call symbols with surrounding scope tokens (function calls inside `port_free_on_agent(agent_url: ...)` no longer mangle to `hml_fn_agent_*`); macOS libwebsockets initialization picks up Homebrew CA bundles by default (no SSL_CERT_FILE workaround); default one-shot LWS HTTP timeout lowered from ~30s to 5s so a single failed call doesn't wedge the caller for half a minute (the `_timeout` variants still honor their per-call timeout); side-effect import workaround documented
 *   2.3.1 - Binary HTTP fixes: @stdlib/http.download() now actually writes the buffer body (was f.write(buffer) which silently no-op'd in compiled binaries and threw in the interpreter); new download_streaming(url, path) for bounded-memory pulls of large files (model weights, archives) — chunks straight to disk via .partial + atomic rename; new stream.read_binary() + __lws_http_stream_read_binary returning buffer chunks (existing read() routes through val_string which strlen-truncates at the first 0x00 byte, corrupting binary payloads)
 *   2.3.0 - Streaming HTTP support in @stdlib/http (chunked / SSE): stream(), stream_get(), stream_post(), post_json_stream(), stream_sse(); compiler runtime now sends POST bodies for streaming requests and throws the same catchable errors as the interpreter (invalid ptr, bad URL, connection failure) for full interpreter/compiler parity
 *   2.2.3 - Patch release with runtime/compiler hardening: catchable socket connect/bind failures, correct /proc and /sys File.read() behavior, safer buffer memory builtins, optional-chain null-guard narrowing, string concatenation numeric regressions fixed, CLI help parsing fixed, and fs.make_dirs() added
 *   2.2.2 - Release prep with compiler/runtime/install hardening: catchable runtime file_stat() failures, typed-array fast-path assignment checks, non-default-prefix stdlib/runtime lookup fixes, and refreshed documentation audits
 *   2.2.1 - HTTP POST/PUT/PATCH/DELETE body fix: compiled binaries actually send Content-Type, Content-Length, and the body bytes (interpreter was already correct); libwebsockets startup banner suppressed by default in compiled binaries to match interpreter
 *   2.2.0 - posix_spawn() primitive in @stdlib/process; fs.open_fd()/fileno() for raw fd access; string-literal object keys and obj?[key] safe-index; FFI fix for two-library import collision; compiler-strictness parity fixes (typed array of custom types, `export let X = X;` self-rebind, substr() arity); incremental builds correctly track header dependencies
 *   2.1.1 - Parser no longer hangs on malformed `match { arm => { ... } }` when block arm is parsed as object literal and fails
 *   2.1.0 - HTTP client actually sends POST/PUT/PATCH bodies; @stdlib/json exports its public API for `import * as`
 *   2.0.3 - Fix compiler symbol collision when a module exports `init` (or any name shared with the module initializer)
 *   2.0.2 - Fix i32 overflow in HashMap djb2 string hash; accumulate in i64 + mask
 *   2.0.1 - Bugfix release since v2.0.0 (runtime/compiler correctness, stability, docs)
 *   2.0.0 - BREAKING: Move 63 builtins to stdlib modules, reducing global namespace conflicts
 *   1.10.0 - (skipped, became 2.0.0)
 *   1.9.4 - Fix fire-and-forget spawn, WebSocket server SO_REUSEADDR
 *   1.9.3 - WebSocket binary data support: __lws_msg_binary builtin, fix server send_binary, fix recv binary handler
 *   1.9.2 - Compiler fixes: unboxed loop counter boxing, clear() object dispatch, exec() import shadowing
 *   1.9.1 - QoL: write() builtin, single-arg slice(), rune join(), HashMap numeric key coercion
 *   1.9.0 - WASM interpreter release artifact, version bump for release
 *   1.8.8 - Fix compiler inlining bugs, ptr subtraction type checking, catchable open() exceptions
 *   1.8.7 - Fix multi-argument print/eprint in compiler codegen
 *   1.8.6 - Fix segfault in hml_string_append_inplace for SSO strings
 *   1.8.5 - 5 new array methods (every, some, indexOf, sort, fill), major performance optimizations, memory leak fixes
 *   1.8.4 - Graceful handling for reserved keywords (def, func, var, class), fix flaky CI tests
 *   1.8.3 - Code polish: consolidate magic numbers, standardize error messages
 *   1.8.2 - Memory leak prevention: exception-safe eval, task/channel cleanup, optimizer fixes
 *   1.8.1 - Fix use-after-free bug in function return value handling
 *   1.8.0 - Pattern matching, arena allocator, memory leak fixes
 *   1.7.5 - Fix formatter else-if indentation bug
 *   1.7.4 - Formatter improvements: function parameter, binary expr, import, and method chain line breaking
 *   1.7.3 - Fix formatter comment and blank line preservation
 *   1.7.2 - Maintenance release
 *   1.7.1 - Single-line if/while/for statements (braceless syntax)
 *   1.7.0 - Type aliases, function types, const params, method signatures, loop labels, named args, null coalescing
 *   1.6.7 - Octal literals, block comments, hex/unicode escapes, numeric separators
 *   1.6.6 - Float literals without leading zero, fix strength reduction bug
 *   1.6.5 - Fix for-in loop syntax without 'let' keyword
 *   1.6.4 - Hotfix release
 *   1.6.3 - Fix runtime method dispatch for file, channel, socket types
 *   1.6.2 - Patch release
 *   1.6.1 - Patch release
 *   1.6.0 - Compile-time type checking, LSP integration, bitwise operators
 *   1.5.0 - Full type system, async/await, atomics, 39 stdlib modules
 *   1.1.0 - Unified versioning across all components
 *   1.0.x - Initial release series (interpreter only)
 *   0.1.x - Pre-release compiler development
 */

#endif /* HEMLOCK_VERSION_H */
